diff --git a/modules/juce_gui_basics/menus/juce_PopupMenu.cpp b/modules/juce_gui_basics/menus/juce_PopupMenu.cpp
index a213894718..0b24219af7 100644
--- a/modules/juce_gui_basics/menus/juce_PopupMenu.cpp
+++ b/modules/juce_gui_basics/menus/juce_PopupMenu.cpp
@@ -986,6 +986,14 @@ struct MenuWindow final : public Component, private AsyncUpdater
                 tendTowardsRight = (parentArea.getRight() - target.getRight()) >= (target.getX() - parentArea.getX());
             }
 
+            if (parent != nullptr)
+            {
+                if (options.getPreferredSubmenuDirection() == Options::SubmenuDirection::towardsLeft)
+                    tendTowardsRight = false;
+                else if (options.getPreferredSubmenuDirection() == Options::SubmenuDirection::towardsRight)
+                    tendTowardsRight = true;
+            }
+
             x = tendTowardsRight ? jmin (parentArea.getRight() - widthToUse - 4, target.getRight())
                                  : jmax (parentArea.getX() + 4, target.getX() - widthToUse);
 
@@ -2183,6 +2191,11 @@ PopupMenu::Options PopupMenu::Options::withPreferredPopupDirection (PopupDirecti
     return with (*this, &Options::preferredPopupDirection, direction);
 }
 
+PopupMenu::Options PopupMenu::Options::withPreferredSubmenuDirection (SubmenuDirection direction) const
+{
+    return with (*this, &Options::preferredSubmenuDirection, direction);
+}
+
 PopupMenu::Options PopupMenu::Options::withInitiallySelectedItem (int idOfItemToBeSelected) const
 {
     return with (*this, &Options::initiallySelectedItemId, idOfItemToBeSelected);
diff --git a/modules/juce_gui_basics/menus/juce_PopupMenu.h b/modules/juce_gui_basics/menus/juce_PopupMenu.h
index d5e9d181eb..5568a5dd78 100644
--- a/modules/juce_gui_basics/menus/juce_PopupMenu.h
+++ b/modules/juce_gui_basics/menus/juce_PopupMenu.h
@@ -477,6 +477,13 @@ public:
             downwards
         };
 
+        enum class SubmenuDirection
+        {
+            automatic,
+            towardsLeft,
+            towardsRight
+        };
+
         //==============================================================================
         /** Sets the target component to use when displaying the menu.
 
@@ -564,6 +571,9 @@ public:
         /** Sets the direction of the popup menu relative to the target screen area. */
         [[nodiscard]] Options withPreferredPopupDirection (PopupDirection direction) const;
 
+        /** Sets the horizontal side used by hover submenus. */
+        [[nodiscard]] Options withPreferredSubmenuDirection (SubmenuDirection direction) const;
+
         /** Sets an item to select in the menu.
 
             This is useful for controls such as combo boxes, where opening the combo box
@@ -650,6 +660,9 @@ public:
         */
         PopupDirection getPreferredPopupDirection() const noexcept   { return preferredPopupDirection; }
 
+        /** Gets the preferred horizontal side for hover submenus. */
+        SubmenuDirection getPreferredSubmenuDirection() const noexcept { return preferredSubmenuDirection; }
+
         /** Gets the ID of the item that must be selected when the menu is initially shown.
 
             @see withItemThatMustBeVisible
@@ -663,6 +676,7 @@ public:
         int visibleItemID = 0, minWidth = 0, minColumns = 1, maxColumns = 0, standardHeight = 0, initiallySelectedItemId = 0;
         bool isWatchingForDeletion = false;
         PopupDirection preferredPopupDirection = PopupDirection::downwards;
+        SubmenuDirection preferredSubmenuDirection = SubmenuDirection::automatic;
     };
 
     //==============================================================================
